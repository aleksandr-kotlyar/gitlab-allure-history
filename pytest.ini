[pytest]

addopts =
    --alluredir=allure-results
    --allure-link-pattern=link:https://github.com/aleksandr-kotlyar/gitlab-allure-history/issues/{}

markers =
    smoke: fast critical checks for pipeline gate
    regression: broader stable regression checks for pipeline gate
    demo: demonstration tests that may intentionally fail
